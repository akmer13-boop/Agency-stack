# Stage 3.3B — локальная синхронизация Bitrix24

Статус: Stage 3.3B.1 принят локально на полном Run #3; Stage 3.3B.2 начат в версии 0.3.8.

## Сетевой фундамент

- единый SOCKS5-прокси для Telegram, OpenAI и Bitrix24;
- поддержка IP-whitelist без логина и пароля;
- секреты прокси не выводятся в логи;
- локально на Windows подтверждены Telegram, OpenAI и Bitrix24 через выделенный IP;
- запись в CRM запрещена: `ALLOW_CRM_WRITE=false`.

## Stage 3.3B.1 — полная локальная CRM-синхронизация

Команды:

```text
/bitrix_sync
/bitrix_sync_status
```

Read-only full sync сохраняет в локальную SQLite:

- сделки;
- лиды;
- контакты;
- компании;
- CRM-активности;
- историю стадий сделок;
- историю стадий лидов.

Хранение выполняется в `crm_raw_entities`:

- тип сущности;
- ID сущности;
- JSON payload;
- SHA-256 payload;
- время изменения из Bitrix24, когда оно доступно;
- время локальной синхронизации.

История и прогресс запусков хранятся в `crm_sync_runs`.

### Финальная локальная приёмка 0.3.7

Run #3 успешно прошёл до реального конца данных по ID-cursor:

- сделки — 8 114;
- лиды — 18 289;
- контакты — 39 279;
- компании — 1 805;
- активности — 117 439;
- история стадий сделок — 34 781;
- история стадий лидов — 63 210.

Старый искусственный предел 10 000 снят. Полный проход подтвердил работу ID-cursor на больших выборках.

Для больших выборок используется cursor-подход:

1. сортировка по `ID ASC`;
2. первая страница с `start=-1`;
3. следующая страница с фильтром `>ID` последней полученной записи;
4. повтор до реального конца данных;
5. каждая страница сразу сохраняется в SQLite.

`BITRIX24_SYNC_MAX_ITEMS_PER_ENTITY=0` означает отсутствие искусственного лимита по количеству записей. Положительное значение можно использовать только как диагностический/test limit.

Сохраняется отдельный аварийный предел количества страниц для защиты от зацикливания или некорректного ответа API.

## Stage 3.3B.2 — Incremental CRM Sync 0.3.8

Добавлена команда:

```text
/bitrix_sync_incremental
```

Incremental sync использует начало последнего успешно завершённого sync как checkpoint и вычитает защитный overlap. По умолчанию overlap равен 5 минутам.

Фильтры:

- сделки — `DATE_MODIFY`;
- лиды — `DATE_MODIFY`;
- контакты — `DATE_MODIFY`;
- компании — `DATE_MODIFY`;
- активности — `LAST_UPDATED`;
- история стадий сделок и лидов — `CREATED_TIME`.

Incremental выборка также идёт по ID-cursor внутри окна изменений. Это позволяет не перечитывать сотни тысяч старых записей на каждом обычном запуске.

Настройки:

```env
BITRIX24_SYNC_MAX_PAGES=20000
BITRIX24_SYNC_MAX_ITEMS_PER_ENTITY=0
BITRIX24_SYNC_TIMEOUT_SECONDS=60
BITRIX24_SYNC_RETRY_ATTEMPTS=4
BITRIX24_SYNC_RETRY_BACKOFF_SECONDS=2
BITRIX24_SYNC_PAGE_DELAY_SECONDS=0.25
BITRIX24_SYNC_OVERLAP_MINUTES=5
```

Retry выполняется для временных transport/server/limit ошибок. Полный webhook URL не должен попадать в INFO-лог `httpx`.

## Ограничения Stage 3.3B.2

- удалённые из Bitrix24 сущности пока не превращаются в локальные tombstone-записи; для этого нужен отдельный reconciliation/event механизм;
- таймлайн-комментарии не загружаются массово — это будет отдельный контролируемый обход;
- `COMMUNICATIONS` и файлы CRM-активностей пока явно не выбираются;
- задачи, Открытые линии, телефония, сотрудники и Диск остаются закрыты текущими правами вебхука;
- нормализованный аналитический слой ещё не построен.

## Локальная приёмка 0.3.8

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m app.telegram.bot
```

В Telegram:

```text
/status
/bitrix_sync_incremental
/bitrix_sync_status
```

Критерий приёмки: incremental run должен завершиться существенно быстрее full sync и вернуть только записи из окна изменений плюс overlap, при этом общие локальные счётчики не должны уменьшаться.

## Следующие задачи Stage 3.3B.2

1. безопасный обход комментариев таймлайна;
2. товарные позиции и смарт-процессы;
3. нормализованный аналитический слой поверх raw-хранилища;
4. reconciliation удалений;
5. подключение ИИ-РОПа к локальному аналитическому слою, а не к сырому Bitrix24 напрямую.
