# Local helper scripts

- `setup-local.sh` — macOS/Linux bootstrap.
- `test-agent.sh` — safe local API smoke test.
- `setup-local.ps1` — Windows bootstrap.

On macOS/Linux run shell scripts through `bash scripts/<name>.sh` or mark them executable locally with `chmod +x scripts/*.sh`.
